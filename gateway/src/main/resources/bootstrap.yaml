security:
  oauth2:
    client:
      clientId: client_id
      clientSecret: secret
spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8088
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka