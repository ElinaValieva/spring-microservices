group 'org.example'
version '1.0-SNAPSHOT'

task setup {
    doFirst {
        subprojects {
            println("Build project: ${it.name}")
            exec {
                commandLine('cmd', '/c', 'gradlew build')
            }
        }
    }
}