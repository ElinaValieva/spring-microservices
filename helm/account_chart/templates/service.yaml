apiVersion: v1
kind: Service
metadata:
  name: {{ include "account_chart.fullname" . }}
  labels:
    {{- include "account_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "account_chart.selectorLabels" . | nindent 4 }}
