apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "account_chart.fullname" . }}-test-connection"
  labels:
    {{- include "account_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "account_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
