zuul:
  prefix: /api
server:
  port: 8008
spring:
  application:
    name: gateway
  boot:
    admin:
      client:
        url: http://admin/
management:
  endpoints:
    web:
      exposure:
        include: "*"